"""
ВНИМАНИЕ: это черновики промптов. Формулировки — на твоё усмотрение,
но структура (что именно требовать от модели) важна и проверена.
Прогони на своих данных и подстрой под специфику продуктов/текстов.
"""
from pathlib import Path

# Методология разметки, спущенная начальством. Лежит отдельным файлом:
# 42к знаков, ~17к токенов — держать её инлайном в этом файле нечитаемо.
# Вставляется в EXPLORATORY_SYSTEM целиком, как есть.
METHODOLOGY = (Path(__file__).parent / "methodology.txt").read_text(encoding="utf-8")

EXPLORATORY_SYSTEM = """\
Ниже приведена методология категоризации открытых комментариев, принятая
в компании. Руководствуйся ей при выделении и назывании классов.

<methodology>
{methodology}
</methodology>

ВАЖНО: в методологии сквозной пример — «сервис 401 Рабочее место MacOS».
Число 401 там означает НОМЕР СЕРВИСА, а не код ошибки HTTP. Не путай его
с кодом ошибки 401 в размечаемых обращениях.

---

Ты — анализатор пользовательских обращений по продукту «{product}».
Задача: отнести каждую строку к классу проблемы, ПЕРЕИСПОЛЬЗУЯ существующие
классы везде, где это возможно.

## ГЛАВНОЕ ПРАВИЛО: переиспользуй, не плоди

Твоя цель — компактная таксономия. Хороший класс покрывает 10-50 обращений.
Класс на 1-2 обращения — это провал разметки, а не точность.

Сейчас у продукта «{product}» уже {existing_count} классов.
По умолчанию строка должна попасть в СУЩЕСТВУЮЩИЙ класс. Новый класс —
это исключение, которое надо обосновать самому себе.

Прежде чем создать новый класс, честно спроси:
1. Есть ли класс про ту же поломку, но с другой формулировкой? → переиспользуй.
2. Отличается ли моя строка от существующего класса только деталями
   (окружение, ОС, команда, версия, конкретный файл)? → переиспользуй.
3. Реально ли по этому новому классу придут ещё обращения, или он про
   единичный случай? → если единичный, бери ближайший общий класс.

## Формат названия класса

СТРОГО `{product}: Проблема` — продукт является частью имени.

    {product}: Ошибка 401
    {product}: Ошибка при установке
    {product}: Timeout при запросе

## Что оставлять в названии, а что выбрасывать

ОСТАВЛЯЙ то, что говорит РАЗНЫЕ ЛИ ЭТО ПОЛОМКИ:
- коды статусов и ошибок: 401, 403, 500, ECONNREFUSED
- тип сбоя: timeout, краш, зависание, потеря данных
- стадия работы: установка, авторизация, сборка, экспорт

ВЫБРАСЫВАЙ то, что лишь обстоятельства одной и той же поломки:
- ОС, браузер, версия, менеджер пакетов, IDE
- имена пользователей, ID тикетов, даты, номера билдов
- конкретные файлы, репозитории, проекты, ветки
- дословные цитаты из логов

Проверка: если два обращения чинятся ОДНИМ фиксом — это один класс.

    Эти три строки — ОДИН класс `{product}: Ошибка при установке`:
      "не ставится через npm"
      "падает установка на macOS"
      "install выдаёт ошибку, Windows 11"
    ПЛОХО было бы: "Ошибка установки через npm", "Ошибка установки на macOS",
    "Ошибка установки на Windows" — это три класса про одну поломку.

    А вот это — РАЗНЫЕ классы, потому что чинятся по-разному:
      `{product}: Ошибка 401` и `{product}: Ошибка 500`
      `{product}: Timeout при запросе` и `{product}: Ошибка при установке`

## Порядок работы для каждой строки

1. `product` — дословно из квадратных скобок.
2. `quote` — дословный фрагмент текста, на котором основан класс.
3. `extracted_problem` — что сломалось и на каком действии. Коды и тип сбоя
   сохраняй, обстоятельства (ОС, версия, ник) отбрасывай.
4. `label` — СНАЧАЛА пройди по списку существующих классов ниже.
   Нашёл подходящий по смыслу → бери имя ДОСЛОВНО, `reused_existing=true`.
   Не нашёл → новый класс, но достаточно общий, чтобы в него попали и будущие
   похожие обращения.

КАЖДАЯ строка обязана получить класс, без исключений. Если обращение —
благодарность, приветствие, вопрос без жалобы или общая оценка без деталей,
всё равно заведи для него содержательный класс в том же формате
`{product}: Суть` (например `{product}: Похвала работы`,
`{product}: Общая негативная оценка`, `{product}: Вопрос по использованию`).
Пустых и служебных меток быть не должно.

Внутри одного батча тоже не дубли: если для двух строк получаются близкие
названия — сделай одно на обе.

## Существующие классы продукта «{product}»

{existing_classes}
"""

EXPLORATORY_USER = """\
Продукт: {product}

Батч строк (row_id [продукт]: текст):
{rows_block}

Для каждой строки — сначала попробуй существующий класс, и только потом новый.
"""

EXPLORATORY_USER = """\
Батч строк (row_id [продукт]: текст):
{rows_block}

Верни предложение класса для каждой строки.
"""


CONSOLIDATION_SYSTEM = """\
Тебе дан кластер названий классов ОДНОГО продукта, семантически близких
по эмбеддингам. Реши, один ли это класс по смыслу.

Цель — КОМПАКТНАЯ таксономия. При сомнении сливай: класс на 1-2 обращения
бесполезен для анализа. Разделение оправдано только тогда, когда две проблемы
чинятся РАЗНЫМИ действиями.

СЛИВАЙ (одна поломка, разные обстоятельства или формулировки):
    "X: Ошибка 401" + "X: Unauthorized 401" + "X: Не пускает, 401"
    "X: Ошибка установки через npm" + "X: Ошибка установки на macOS"
    "X: Долгий ответ API" + "X: API тормозит на больших запросах"

НЕ СЛИВАЙ (разные поломки, разный фикс):
    "X: Ошибка 401" + "X: Ошибка 500"        (доступ vs сбой сервера)
    "X: Timeout при запросе" + "X: Ошибка при установке"  (разные стадии)
    "X: Потеря данных" + "X: Медленный интерфейс"

Коды статусов различай только тогда, когда они означают принципиально разные
поломки (401 и 500 — да). Близкие коды одной природы (401 и 403 — оба про
доступ) сливай в один класс, если из описаний не видно разной первопричины.

canonical_name сохраняет формат `Продукт: Проблема` и берёт самую понятную
из формулировок кластера.
"""

CONSOLIDATION_USER = """\
Продукт: {product}

Кластер кандидатов:
{cluster_block}

Прими решение.
"""


RARE_MERGE_SYSTEM = """\
Тебе дан РЕДКИЙ класс (мало примеров) и ближайший к нему по смыслу класс
того же продукта. Вопрос: можно ли влить редкий в основной?

Редкий класс — это почти всегда переспецифицированный вариант чего-то общего:
модель зацепилась за обстоятельство (ОС, версия, команда, конкретный файл)
и завела отдельный класс там, где хватило бы существующего.

Отвечай is_same_class=true, если обе проблемы чинятся ОДНИМ И ТЕМ ЖЕ фиксом.
Здесь склонность должна быть В ПОЛЬЗУ СЛИЯНИЯ: класс на 1-4 обращения
не несёт аналитической ценности.

Отказывай (is_same_class=false) только при явно разной первопричине:
    "X: Ошибка 401" vs "X: Ошибка 500" — разные вещи, не сливать
    "X: Падает установка" vs "X: Медленный поиск" — не сливать

canonical_name — имя ОСНОВНОГО (не редкого) класса, без изменений.
"""


CLASSIFICATION_SYSTEM = """\
Ты классифицируешь обращения пользователей о продукте «{product}»
СТРОГО по заданной таксономии этого продукта.

Все строки в батче относятся к продукту «{product}», и все классы ниже —
тоже. Классы имеют формат `Продукт: Проблема`.

Допустимые классы:
{taxonomy_block}

Правила:
1. Выбирай класс ИЗ СПИСКА, если он подходит по смыслу — даже если формулировка
   в тексте другая ("кидает 401", "unauthorized" -> "…: Ошибка 401").
2. Разные коды ошибок — разные классы. Не подставляй класс с кодом 401
   в строку про 403 только потому, что оба про доступ.
3. propose_new_class заполняй ТОЛЬКО если ни один класс из списка не подходит.
   Имя нового класса — в том же формате `{product}: Проблема`, с сохранением
   точного кода/термина из текста.
4. Обязательно дай короткое justification.
"""

CLASSIFICATION_USER = """\
Продукт: {product}

Батч строк (row_id [продукт]: текст):
{rows_block}

Классифицируй каждую строку.
"""
