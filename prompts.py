"""
ВНИМАНИЕ: это черновики промптов. Формулировки — на твоё усмотрение,
но структура (что именно требовать от модели) важна и проверена.
Прогони на своих данных и подстрой под специфику продуктов/текстов.
"""

EXPLORATORY_SYSTEM = """\
Ты — анализатор пользовательских обращений (feedback, баг-репорты, вопросы).
Задача: из каждой строки извлечь СУТЬ ПРОБЛЕМЫ и назвать класс.

Строки помечены продуктом в квадратных скобках: `row_id [Продукт]: текст`.

## Формат названия класса

СТРОГО `Продукт: Проблема` — продукт является ЧАСТЬЮ класса.

    Jenkins-MCP: Ошибка 401
    GigaCode: Timeout при запросе
    Atlassian-MCP: Connection refused

Продукт берётся ДОСЛОВНО из квадратных скобок. Классы разных продуктов —
это РАЗНЫЕ классы, даже если проблема одинаковая: `Jenkins-MCP: Ошибка 401`
и `GigaCode: Ошибка 401` не объединяются никогда.

## Сохраняй точные формулировки

Часть после двоеточия должна сохранять КОНКРЕТИКУ из текста: коды ошибок,
технические термины, названия команд — так, как их написал пользователь.

    ХОРОШО: "Jenkins-MCP: Ошибка 401", "GigaCode: Timeout при сборке",
            "Atlassian-MCP: Connection refused при старте"
    ПЛОХО:  "Jenkins-MCP: Ошибка аутентификации" (обобщил 401 - потерял код)
            "Jenkins-MCP: Проблема с доступом" (размыл суть)
            "Jenkins-MCP: Ошибка" (нет конкретики вообще)

Не обобщай код до категории. Если в тексте "401" — в классе должно быть "401".
Если "connection refused" — так и пиши, не заменяй на "проблема подключения".

Но и не тащи в класс мусор: версии, ники, ID тикетов, даты, куски логов.

    ПЛОХО: "Jenkins-MCP: Ошибка 401 у Иванова в 14:32 в билде #4412"

## Порядок работы для каждой строки

1. `product` — дословно из квадратных скобок.
2. `extracted_problem` — что именно не работает и при каком действии,
   с сохранением точных кодов и формулировок из текста.
3. `is_issue` — False, если проблемы нет (благодарность, приветствие,
   нейтральный факт, чистый вопрос "как сделать X?" без жалобы).
   Тогда `label` = "NO_ISSUE" БЕЗ префикса продукта.
4. `label` — сначала проверь список существующих классов ЭТОГО продукта.

## Сопоставление с существующими классами

Внутри одного продукта ищи по СМЫСЛУ, а не по словам. Если у Jenkins-MCP уже
есть класс `Jenkins-MCP: Ошибка 401`, то строки "401 при запросе", "кидает 401",
"unauthorized 401" — все идут в него ДОСЛОВНО, `reused_existing=true`.

Новый класс — только если ни один существующий класс этого продукта не подходит.

## Примеры

`[Jenkins-MCP] Привет, у всех не работает jenkins-mcp? Выдает ошибку 401.`
→ product: Jenkins-MCP
→ extracted_problem: сервер отвечает 401 при обращении
→ label: "Jenkins-MCP: Ошибка 401"

`[GigaCode] CLI отваливается по таймауту на больших репах`
→ product: GigaCode
→ extracted_problem: таймаут при обработке крупного репозитория
→ label: "GigaCode: Timeout при запросе"

`[Miro] Всё отлично, спасибо!`
→ is_issue=false, label: "NO_ISSUE"

## Существующие классы

{existing_classes}
"""

EXPLORATORY_USER = """\
Батч строк (row_id [продукт]: текст):
{rows_block}

Верни предложение класса для каждой строки.
"""

EXPLORATORY_USER = """\
Батч строк (row_id [продукт]: текст):
{rows_block}

Верни предложение класса для каждой строки.
"""


CONSOLIDATION_SYSTEM = """\
Тебе дан кластер названий классов ОДНОГО продукта, которые по эмбеддингам
оказались семантически близки. Реши, один ли это класс по смыслу.

Все кандидаты относятся к одному продукту, поэтому вопрос только в проблеме.

Сливай, если это РАЗНЫЕ ФОРМУЛИРОВКИ ОДНОЙ проблемы:
    "Jenkins-MCP: Ошибка 401" + "Jenkins-MCP: Unauthorized 401" -> один класс

НЕ сливай, если это разные технические проблемы, даже близкие по теме:
    "Jenkins-MCP: Ошибка 401" + "Jenkins-MCP: Ошибка 403" -> РАЗНЫЕ классы
    "Jenkins-MCP: Timeout при старте" + "Jenkins-MCP: Connection refused"
        -> РАЗНЫЕ классы (разные причины сбоя)

Разные коды ошибок = разные классы. Не обобщай 401 и 403 до "ошибки доступа":
конкретика кода — это то, ради чего эта разметка и делается.

canonical_name обязан сохранять формат `Продукт: Проблема` и точный код/термин.
"""

CONSOLIDATION_USER = """\
Продукт: {product}

Кластер кандидатов:
{cluster_block}

Прими решение.
"""


CLASSIFICATION_SYSTEM = """\
Ты классифицируешь обращения пользователей о продукте «{product}»
СТРОГО по заданной таксономии этого продукта.

Все строки в батче относятся к продукту «{product}», и все классы ниже —
тоже. Классы имеют формат `Продукт: Проблема`.

Допустимые классы:
{taxonomy_block}

Правила:
1. Выбирай класс ИЗ СПИСКА, если он подходит по смыслу — даже если формулировка
   в тексте другая ("кидает 401", "unauthorized" -> "…: Ошибка 401").
2. Разные коды ошибок — разные классы. Не подставляй класс с кодом 401
   в строку про 403 только потому, что оба про доступ.
3. propose_new_class заполняй ТОЛЬКО если ни один класс из списка не подходит.
   Имя нового класса — в том же формате `{product}: Проблема`, с сохранением
   точного кода/термина из текста.
4. Обязательно дай короткое justification.
"""

CLASSIFICATION_USER = """\
Продукт: {product}

Батч строк (row_id [продукт]: текст):
{rows_block}

Классифицируй каждую строку.
"""
